<style>
  body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f4f4f4;
      color: #333;
      margin: 0;
      display: flex;
      flex-direction: column; /* Use column layout */
      min-height: 100vh; /* Ensure full height of the viewport */
  }

  main {
      flex-grow: 1; /* Allow main content to expand and fill space */
      display: flex;
      align-items: center; /* Center content vertically */
      justify-content: center; /* Center content horizontally */
  }

  .error-container {
      text-align: center;
      background-color: #fff;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1);
  }

  .error-container h1 {
      font-size: 100px;
      color: #FF6B6B;
      margin: 0;
      animation: bounce 1s infinite; /* Add bounce animation */
  }

  .error-container h2 {
      font-size: 28px;
      margin: 10px 0;
      color: #333;
  }

  .error-container p {
      font-size: 18px;
      color: #666;
  }

  .error-container a {
      display: inline-block;
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #85FF00;
      color: white;
      text-decoration: none;
      font-weight: bold;
      border-radius: 5px;
  }

  .error-container a:hover {
      background-color: #69d800;
  }

  .error-icon {
      font-size: 120px;
      color: #FF6B6B;
      margin-bottom: 20px;
  }

  /* Bounce animation */
  @keyframes bounce {
      0%, 100% {
          transform: translateY(0);
      }
      50% {
          transform: translateY(-20px); /* Bounce height */
      }
  }
</style>

<div class="container mt-5">
  <div class="error-container">
      <div class="error-icon">🚨</div>
      <h1>500</h1>
      <h2>Oops! Something went wrong.</h2>
      <p>We're experiencing some server issues right now. Please try again later.</p>
      
      <% if (errorMessage) { %>
          <p><strong>Error Details:</strong> <%= errorMessage %></p>
      <% } %>
      
      <a href="/">Go Back to Home</a>
  </div>
</div>